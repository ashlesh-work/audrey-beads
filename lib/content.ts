// Thin façade so pages can import content helpers from one place.
export { DEFAULT_CONTENT } from "./defaults";
export { getContent, listProducts as getProducts, listTestimonials as getTestimonials } from "./store";
